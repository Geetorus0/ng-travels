{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @workspace/api-server --filter @workspace/ng-travels run dev",
    "dev:web": "pnpm --filter @workspace/ng-travels run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:sandbox": "pnpm --filter @workspace/mockup-sandbox run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:vercel": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/ng-travels run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
